# Event Assist — Two-Stage Survival Pipeline

Timestamp: **2026-08-11 20:20 JST**

Status: `FROZEN_PROCESS / READY_TO_EXECUTE`

This file binds the Event Assist outcome contract to two separate kill stages.

## Canonical sequence

`PROTOTYPE SWARM`
→ `FEATURE_SPEC — usable outcome/reality contract`
→ `GATE 1: NECESSITY SURVIVAL`
→ `SURVIVING_RESPONSIBILITY_MAP`
→ `MATERIALIZE REALITY_A + REALITY_B`
→ `GATE 2: METEOR CRUCIBLE — Reality Survival Loop`
→ `SURVIVING_IMPLEMENTATION_MAP`
→ `FORMAL SUCCESSOR RTS IMPLEMENTATION`
→ `REAL-WORLD DOGFOOD`
→ `DARWIN LOOP ON NEW MATERIAL FAILURES`

## The two specifications

1. `NECESSITY_SURVIVAL_SPEC.md`
   - kills reasons to build;
   - uses A/B hypotheses plus Raison d’être Destroy Loop;
   - outputs only responsibilities that still need to exist.

2. `METEOR_CRUCIBLE_SPEC.md`
   - kills the actual concrete systems;
   - compares runnable existing composition against the smallest justified custom/glue candidate;
   - outputs only implementations that survive the same frozen workload and adversarial attacks.

## Invariants

`SPECIFICATION SURVIVAL != BUILD AUTHORIZATION`

`NECESSITY SURVIVAL != IMPLEMENTATION SURVIVAL`

`PROTOTYPE != PRODUCTION`

`EXTERNAL FIRST != EXTERNAL FOREVER`

`CUSTOM AVAILABLE != CUSTOM NECESSARY`

`REAL-WORLD FAILURE REOPENS THE LOOP`

The two kill stages must not be collapsed. Doing so lets an attractive implementation smuggle its own necessity into the decision.
