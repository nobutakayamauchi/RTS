# Event Assist — METEOR CRUCIBLE

Timestamp: **2026-08-11 20:20 JST**

Status: `FROZEN_GATE_2 / IMPLEMENTATION_NOT_YET_SURVIVED`

Formal name: **METEOR CRUCIBLE — Reality Survival Loop**

Purpose: kill the concrete implementation after necessity has survived.

Gate 1 asks:

> Should this responsibility exist at all?

METEOR CRUCIBLE asks:

> Can this exact concrete system survive reality better than its alternatives?

## Entry condition

Only responsibility rows that survive `NECESSITY_SURVIVAL_SPEC.md` may enter this gate.

The surviving minimum is materialized into concrete competing candidates:

- `REALITY_A` — strongest existing/external composition that can actually be assembled;
- `REALITY_B` — smallest custom/glue implementation justified by Gate 1;
- hybrid candidates are permitted only when the responsibility map specifically justifies them.

A paper architecture is insufficient. Each candidate must be runnable or otherwise materially exercisable against the frozen workload.

## Frozen reality workload

Use the same Event Assist cases and preservation workload without weakening them:

- rental move-in evidence omission prevention;
- childbirth benefit/procedure omission prevention;
- evidence capture and custody;
- encryption/cloud preservation;
- remote-generation verification;
- original-device loss;
- fresh-environment recovery;
- independent verification;
- authority, privacy, stale-source, rollback, key and notification attacks.

## Reality attack classes

Attack both candidates on:

- outcome correctness;
- false negative / false positive behavior;
- UNKNOWN preservation;
- authoritative-source freshness;
- event and applicability-fact binding;
- evidence integrity / provenance / custody;
- security and privacy blast radius;
- key compromise / recovery;
- provider failure / deletion / rollback;
- watch and scheduler failure;
- operator burden under stress;
- alert fatigue and notification leakage;
- idempotency / retry behavior;
- schema/tool/provider evolution;
- fresh-environment recovery;
- reconstructability without chat memory;
- deployment/runtime identity where software behavior is material;
- whole-life cost and maintenance burden.

## Required adversarial loop

For each material failure:

`ATTACK`
→ `FAIL / SURVIVE / UNKNOWN`
→ `AUTOPSY`
→ `COUNTER-DA`
→ `PATCH / CONTAIN / RECOMPOSE / KILL`
→ `RETEST SAME WORKLOAD`

Do not switch to an easier workload after failure.

## Implementation survival verdicts

Per responsibility row:

- `EXTERNAL_IMPLEMENTATION_SURVIVES`
- `EXTERNAL_PLUS_GLUE_SURVIVES`
- `CUSTOM_MINIMUM_SURVIVES`
- `BOTH_DIE_REOPEN_REQUIREMENT`
- `EVIDENCE_INSUFFICIENT`

The winner is selected per responsibility, not by global ideology.

## Promotion rule

Only the concrete responsibilities that survive METEOR CRUCIBLE may enter the formal successor RTS implementation.

`NECESSITY SURVIVED != IMPLEMENTATION SURVIVED`

`TESTED ONCE != REALITY FIT`

`SURVIVES CRUCIBLE != ETERNALLY SAFE`

Real-world failures reopen the Darwin loop.

## Stop rule

Continue rotated attacks while materially new implementation failure classes appear.
Stop only at:

`SEARCH_SATURATED_UNDER_CURRENT_EVIDENCE`

## Gate 2 verdict

Current state:

`METEOR_CRUCIBLE = NOT_YET_RUN`
