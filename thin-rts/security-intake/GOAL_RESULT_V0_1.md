# /goal Result — Pre-WITNESS / Pre-Promotion Quarantine v1

Status: `ADOPT / FINAL_PROMOTION_CANDIDATE`

## Goal

Prevent hostile or ambiguous external material from entering WITNESS / ULTIMATE LOOP as trusted input, and prevent internally generated material from being promoted outward without an equivalent evidence-bound hygiene check.

## WITNESS result

The problem decomposes into four durable responsibilities:

1. source identity + exact-byte integrity before interpretation;
2. explicit hygiene verdict before WITNESS ingestion;
3. threat death-cause conversion into normalized regression memory rather than raw-payload learning;
4. symmetric pre-promotion egress verification with producer/target identity and exact output evidence.

General malware detection, GitHub credential security, dependency intelligence, secret scanning and branch protection do not survive as custom RTS responsibilities; they remain external controls/challengers to compose.

## ULTIMATE LOOP result

### Loop 1 — Raison d'être Destroy

Verdict: `SURVIVES_AS_MINIMUM_CONTRACT`.

Frozen invariants:

`NO CLEAN EVIDENCE-BOUND INTAKE VERDICT -> NO WITNESS INGESTION`

`NO CLEAN EVIDENCE-BOUND EGRESS VERDICT -> NO PROMOTION`

### Loop 2 — METEOR CRUCIBLE

DA / Counter-DA killed and repaired:

- GlassWorm-specific architecture;
- blanket Variation Selector bans that break legitimate emoji prose;
- `.github` scanner exclusion;
- post-ingestion scanning;
- raw-threat-to-learning coupling;
- silent unsupported-file admission;
- regression fixtures containing literal hostile/invisible bytes;
- asymmetric intake-only enforcement;
- egress adapters that existed but were not exercised on the promotion surface;
- zero-finding external scans without a positive detection self-test;
- loose CI dependency/credential handling;
- an internally inconsistent 110-point rubric presented as `/100`.

Current result: `EXTERNAL_CONTROLS + THIN_GLUE_SURVIVES_CURRENT_EVIDENCE`.

### Loop 3 — DARWIN ARENA

The Python scanner and Semgrep composition are replaceable occupants. The durable objects are:

- quarantine contract;
- evidence identities/hashes;
- input/output symmetry;
- raw-evidence vs learning separation;
- inherited threat regressions / known death causes.

## Implementation produced

- hardened `scripts/check_invisible_unicode.py`;
- shared `scripts/quarantine_core.py`;
- `scripts/intake_quarantine.py`;
- `scripts/egress_quarantine.py`;
- `scripts/repository_egress_gate.py` with aggregate manifest evidence;
- 14 focused symmetric regression tests;
- `.semgrep/security-intake.yml` independent challenger rules;
- `scripts/verify_semgrep_challenger.py` positive/negative challenger self-test;
- hardened GitHub Actions workflow with read-only permission, full-SHA Action pins and non-persisted checkout credentials;
- `GLASSWORM-VS-001` inherited threat regression capsule;
- DA / Counter-DA autopsy including the historical design-coverage debt and evaluator-score defect.

## GitHub evidence

PR: `#317 feat: pre-WITNESS intake quarantine v0`

Latest completed material validation before this final record update: GitHub Actions run `#1077`.

### Primary Unicode / boundary job

- `Run invisible unicode guard`: `success`;
- symmetric regression suite: `14 tests`, `OK`;
- repository promotion egress gate: `success`;
- checked files: `1015`;
- clean files: `1015`;
- blocked files: `0`;
- egress manifest SHA-256: `0210049ff99512210237061ca48f58b26c754cb6e36e4a1d9102e65672a28116`.

### Independent challenger job

- pinned Semgrep engine `1.172.0` installed successfully;
- challenger self-test accepted the clean fixture;
- challenger self-test rejected Python `eval`;
- challenger self-test rejected Python shell execution;
- challenger self-test rejected JavaScript `eval`;
- repository scan completed successfully with `0 blocking` findings;
- scan covered 1021 tracked files overall and 183 Python targets for the active Python rules;
- checkout credentials were not persisted;
- workflow token permission remained read-only.

## Corrected frozen acceptance rubric

This rubric replaces the previous invalid score whose category maxima summed to 110 while being labeled `/100`.

The corrected maxima sum to exactly 100:

- raison d'être / minimality: **8/8**
- correct pre-WITNESS boundary placement: **12/12**
- exact-byte + boundary-identity evidence binding: **10/10**
- known invisible / Variation Selector death-cause coverage: **12/12**
- machine-interpreted / workflow surface coverage: **8/8**
- bounded legitimate-prose false-positive control: **8/8**
- fail-closed explicit intake + egress behavior: **8/8**
- raw-evidence separation + inherited regression memory: **8/8**
- symmetric egress live enforcement + manifest evidence: **13/13**
- independent challenger composition + self-test + CI credential hardening: **13/13**

`FINAL ACCEPTANCE EVIDENCE = 100 / 100`

This is **not** a claim of “100% secure.” It means every requirement in this frozen adoption rubric has current evidence. Unknown future attack classes can still exist and must reopen DA / METEOR / DARWIN when discovered.

## Historical design-gap conclusion

The earlier Unicode Guard had a real coverage gap: the design defended known zero-width/Bidi classes but had not yet been subjected to the current systematic DA process that generates counterexamples and preserves death causes.

The correction is therefore two-layered:

1. patch the concrete missed attack classes now;
2. make every future material miss become an inherited regression obligation so successors cannot silently forget it.

`DISCOVERED MISS -> PERMANENT REGRESSION MEMORY`

## Final adoption verdict

`ADOPT`

Adopt the contract, boundary placement, symmetric enforcement and inherited regression memory into new RTS.

Adopt the current Python/Semgrep implementation only as replaceable occupants in the DARWIN ARENA / Movable Frame. They have no permanent implementation right.

Promotion is authorized by the user once the final documentation head itself re-runs the same CI successfully. A failure on that final head reopens METEOR rather than weakening the workload.
