# Ultimate Loop Lifecycle — DA / Counter-DA Reuse, Extraction, New-Build Gate

Timestamp: **2026-08-13**

Status: `REUSE_FIRST / MINIMAL_LIFECYCLE_BINDER_SURVIVES`

Frozen order:

`KEEP EXISTING -> EXTRACT FROM EXISTING STRUCTURE -> NEW BUILD ONLY IF IRREDUCIBLE -> PROTOTYPE -> METEOR -> PHOENIX -> FINAL RE-GATE`

## 1. Existing holders kept

| Needed responsibility | Existing holder | Verdict |
|---|---|---|
| necessity destruction | WITNESS / Raison d'être Destroy Loop | `KEEP` |
| specification attack / Counter-DA | WITNESS + DA / Counter-DA records | `KEEP` |
| concrete destructive comparison | METEOR CRUCIBLE | `KEEP` |
| post-promotion challenger comparison | MOVABLE FRAME / DARWIN ARENA | `KEEP` |
| creator-independent continuity | PHOENIX LINEAGE | `KEEP` |
| provider-neutral recovery | Continuity | `KEEP` |
| encrypted custody / integrity | Cloud Custody | `KEEP` |
| durable review / lineage | Git + GitHub | `EXTERNAL / KEEP` |
| challenger discovery / crawling | external search, feeds, APIs, AI | `EXTERNALIZE` |
| scheduling / notification | native or external scheduler | `EXTERNALIZE` |
| deployment / failover execution | authorized external operator/tooling | `EXTERNALIZE / AUTHORITY_REQUIRED` |

No new crawler, daemon, database, scheduler, model runtime, deployment engine, backup service, cloud, crypto or autonomous promotion system survives.

## 2. Structural extraction

The existing Material Transition Record already provides:

- stable identity;
- intent;
- source/evidence reference;
- authority scope;
- action/outcome references;
- PASS / FAIL / BLOCKED / UNKNOWN;
- review versus promotion separation;
- next-state linkage.

The existing DARWIN specification already defines challenger triggers, same-workload comparison, whole-life fitness, shadow mode, partial replacement, migration and lineage.

The existing PHOENIX specification already defines Succession Packets and creator-independent regeneration.

Therefore the new lifecycle layer must not recreate those systems.

## 3. Free-form-only attack

Attack:

> Record lifecycle decisions as prose and rely on the operator/AI to remember when to reopen BUILD, when to keep a 103% candidate as standby, and when recovery/authority evidence is sufficient.

Verdict: `FAIL`.

Material distinctions that can silently collapse in free-form records include:

- novelty versus a material challenge trigger;
- current versus stale trigger evidence;
- numerical performance gain versus replacement eligibility;
- replacement value versus resilience value;
- SHADOW versus STANDBY versus PRIMARY;
- winning comparison versus promotion authority;
- fallback availability versus proven failover fitness;
- logical labels versus failure-domain independence evidence;
- backup existence versus fresh recoverability;
- recovery proof versus PHOENIX regeneration proof;
- unknown future event versus guessed state transition;
- CORE COMPLETE evidence versus authority to freeze/promote.

The irreducible gap is typed binding and fail-closed state transition validation.

## 4. Surviving new build

`IRREDUCIBLE_NEW_BUILD = ONE PURE LIFECYCLE BINDER / VALIDATOR`

Candidate occupant:

`thin-rts/ultimate-loop/lifecycle.py`

Containment:

- standard library only;
- JSON input / JSON report;
- no network;
- no file mutation beyond reading the supplied case;
- no scheduler;
- no background process;
- no database;
- no provider credentials;
- no deployment/failover action;
- no autonomous promotion;
- no recovery implementation;
- no crawler;
- no model invocation.

It only binds externally supplied evidence into lifecycle state and refuses unsafe substitutions.

## 5. Threshold Counter-DA

The prototype carries screening priors of 5% / 15% / 30% for observe / serious comparison / full-replacement consideration.

These are **not universal promotion thresholds**.

A candidate cannot become PRIMARY from a percentage alone. Same-workload survival, migration/rollback fitness, authority and workload-specific evidence remain mandatory.

Thresholds are policy inputs and may be recalibrated or removed when real evidence justifies it.

## 6. Small-win preservation

A candidate that is only slightly better, or whose stability evidence is incomplete, may still have high resilience value.

Example:

`103% performance + unproven stability + recovery probe PASS + high resilience`

must be allowed to remain `STANDBY` rather than being either forced into PRIMARY or discarded.

This preserves a ready alternate path for future service loss without paying unnecessary migration cost today.

## 7. Recovery / Phoenix separation

Hard distinction retained:

`BACKUP EXISTS != RECOVERABLE`

`RECOVERABLE != PHOENIX_READY`

Fresh restore evidence and canonical-material verification are required for recoverability. Creator-independent regeneration additionally requires a sufficient Succession Packet and an independent Phoenix Test.

## 8. Gate verdict

`NEW ULTIMATE LOOP PLATFORM = DEAD`

`EXISTING WITNESS / METEOR / DARWIN / PHOENIX / CONTINUITY = REUSED`

`LIFECYCLE STATE / CANDIDATE DISPOSITION / WATCH ROUTING = EXTRACTED AS TYPED CONTRACT`

`PURE LIFECYCLE VALIDATOR = SURVIVES AS MINIMAL PROTOTYPE`

`PROTOTYPE DESTRUCTIVE METEOR = AUTHORIZED`
